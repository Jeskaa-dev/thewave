<%= simple_form_for @portfolio, url: update_step_portfolio_path(step: step), method: :patch, html: { data: { turbo_frame: "step-#{step}" } } do |f| %>
  <div class="flex flex-col gap-4 bg-gray-50 p-4 rounded-md shadow">
    <h3 class="text-lg font-medium">Étape <%= step %></h3>
    <div>
      <%= f.input "step_#{step}_github_url".to_sym,
                  label: "Lien GitHub",
                  input_html: { class: "input focus:ring-2 focus:ring-green-500 w-full rounded border-gray-300" } %>
    </div>
    <div>
      <%= f.input "step_#{step}_site_url".to_sym,
                  label: "Lien Site",
                  input_html: { class: "input focus:ring-2 focus:ring-green-500 w-full rounded border-gray-300" } %>
    </div>
    <div>
      <%= f.button :submit, "Sauvegarder", class: "btn bg-green-500 text-white py-2 px-4 rounded hover:bg-green-600" %>
    </div>
  </div>
<% end %>
